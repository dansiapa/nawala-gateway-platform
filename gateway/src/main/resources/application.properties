# Nawala Gateway Configuration
spring.application.name=nawala-gateway

# Server
server.port=9090

# Redis for rate limiting
spring.data.redis.host=localhost
spring.data.redis.port=6379

# JWT
nawala.gateway.jwt.secret=TmF3YWxhR2F0ZXdheVNlY3JldEtleUZvckpXVFRva2VuU2lnbjEyMzQ=
nawala.gateway.jwt.expiration-ms=3600000

# Platform service URL
nawala.gateway.platform-url=http://localhost:8080

# Internal API shared secret (must match platform's nawala.internal.secret)
nawala.gateway.internal-secret=NawalaInternalSecretKey2024!

# Payload encryption master key (AES-256, base64-encoded)
nawala.gateway.payload-key=ieUfr//mm5I3ZRW39jagiPW9KG8SOiPgvQZULD00LW4=

# Logging is configured via logback-spring.xml

